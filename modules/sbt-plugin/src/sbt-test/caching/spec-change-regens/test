# First compile generates. Mutate the spec file (preserving semantics
# but changing bytes), compile again. The second compile must regen.
> compile
> snapshotAccountsApi
$ sleep 1100
> mutateSpec
> compile
> verifyRegen
